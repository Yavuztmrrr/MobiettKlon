<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:padding="@dimen/btnLoginValidPadding">
    <solid android:color="@color/gray" />
    <corners
        android:bottomRightRadius="@dimen/btnLoginValid"
        android:bottomLeftRadius="@dimen/btnLoginValid"
        android:topLeftRadius="@dimen/btnLoginValid"
        android:topRightRadius="@dimen/btnLoginValid" />
    <selector>

    </selector>
</shape>