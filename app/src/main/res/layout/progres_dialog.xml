<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ProgressBar
        android:id="@+id/mProgressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="@dimen/progressDialogWidth"
        android:layout_height="@dimen/progressDialogHeight"
        android:layout_marginTop="@dimen/progressDialogMargin"
        android:indeterminateDrawable="@drawable/progress_dialog_tasarim"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.212"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/progressDialogTextHeight"
        android:layout_marginStart="@dimen/progressDialogTextMarginStart"
        android:layout_marginTop="@dimen/progressDialogTextMarginTop"
        android:text="@string/listeYukleniyor"
        android:textColor="@color/kirmizi"
        android:textSize="@dimen/progressDialogTextSize"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.687"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>